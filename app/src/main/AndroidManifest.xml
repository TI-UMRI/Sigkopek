<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.iwanbazz.dev.sigkopek">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="your.package.name.permission.MAPS_RECEIVE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/sipkopas"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ScreenSplashActivity"
            android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

            <!--
            android:screenOrientation="portrait"
            android:configChanges="orientation">
            -->
        </activity>
        <activity
            android:name=".SubKategori.SubPemerintahan"
            android:label="@string/title_activity_sub_kategori1"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SubKategori.SubKesehatan"
            android:label="KESEHATAN" />
        <activity
            android:name=".SubKategori.SubPendidikan"
            android:label="PENDIDIKAN" />
        <activity
            android:name=".SubKategori.SubFasum"
            android:label="FASILITAS UMUM" />
        <activity
            android:name=".SubKategori.Sub_listview.Data_Kelurahan"
            android:label="SUB DATA PEMERINTAHAN" />
        <activity
            android:name=".SubKategori.Sub_listview.Data_Sekolah"
            android:label="SUB DATA PENDIDIKAN" />
        <activity
            android:name=".SubKategori.Sub_listview.Data_Skpd"
            android:label="SUB DATA PEMERINTAHAN" />
        <activity
            android:name=".SubKategori.Sub_listview.Data_Pkm"
            android:label="SUB DATA KESEHATAN" />
        <activity
            android:name=".Pencarian"
            android:label="PENCARIAN" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.maps.v4.API_KEY"
            android:value="  AIzaSyBYXy-TUi52qZouGsSfAy4OP0q5W1GWMCU" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->

        <activity
            android:name=".SubKategori.Detail"
            android:label="Detail" />
        <activity
            android:name=".SubKategori.DetailCari"
            android:label="Detail" />
        <activity
            android:name=".Cari"
            android:label="Detail" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity2"
            android:label="@string/title_activity_maps2" />
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
    </application>

</manifest>